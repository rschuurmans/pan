{{!-- this file contains the html for the module overview page
	functions:
		- go live
		- add modules by pressing an empty module
		- get active modules from localstorage
		- get all possible modules from a json file
		- modules are diveded in 4 categories, source, rythm, modulate and filter
		- still play music in this screen

	go to page:
	- module detail
	- room overview

	current state:
	- html grid is included, 
	- no css, scss files are created
	- no behaviour

 --}}
{{> header header}}


	<ul class='clear-list'>
		{{#each user.modules}}
			
			<ul class='grid_row clear-list'>
				{{#each module}}
				
					{{#if active}}
					<li class='grid_row_item grid_box module_box draggable fn-draggable' data-dragTarget='fn-delete-box' clonable='true' drag-status='none' module-id={{id}} data-col={{col}} data-category={{category}}>  <a href='/play/module/{{id}}' style='background-color:{{color}}'  class="grid_title_center">{{title}}</a></li>

					{{else}}
					
					<li class='grid_row_item  grid_box  module_box' ><a href="/play/add?category={{../category}}&col={{col}}" ><p class="grid_title_center">x</p></li>
					{{/if}}

				
				{{/each}}
			</ul>
		
	{{/each}}
	</ul>

	<div class="fn-delete-box buttons_area"></div>